# set PATH
export PATH=/bin:/usr/bin:/usr/local/bin:${PATH}

#personal aliases
if [ -f $MY_ENV_HOME/.aliases ]; then
   source $MY_ENV_HOME/.aliases
fi

#home computer aliases
if [ -f $HOME/.aliases ]; then
   source $HOME/.aliases
fi
